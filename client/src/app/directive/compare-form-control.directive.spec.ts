import { CompareFormControlDirective } from './compare-form-control.directive';

describe('CompareFormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareFormControlDirective();
    expect(directive).toBeTruthy();
  });
});
